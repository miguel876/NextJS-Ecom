name: NextJS CI

on:
  pull_request:
    branches: ['main', 'dev']

jobs:
  verify:
    runs-on: ubuntu-latest

    steps:
      - run: |
          npm run lint
          npm test
          npm run build
